version: "3"

services:
  apache:
    # You can choose between build the image by yourself or use
    # the image already builted. Just comment one line and uncomment the other

    # image: viniciusbig/cakephp3:v1
    build: .
    env_file: .env
    ports:
      - "${APACHE_PORT}:80"
      - "${XDEBUG_PORT}:9000"
      # - "35729:35729" # live reload
    volumes:
      - ${PROJECT_ROOT}:/var/www/html:rw,delegated
      - ~/.composer:/var/www/.composer
      - ~/.npm:/var/www/.npm
      - ~/.ssh/id_rsa:/root/.ssh/id_rsa
      # - ~/.nvm:/var/www/.nvm
      # VOLUME PARA DESENVOLVIMENTO DA AZUL
      # - ~/workspace/repo/azulveiculos.com.br/projetos-git/integrador-autoavaliar-git:/var/www/html/wp-content/plugins/integrador-autoavaliar
      # - ~/workspace/repo/azulveiculos.com.br/projetos-git/flatsome-child-git:/var/www/html/wp-content/themes/flatsome-child
      # VOLUME PARA DESENVOLVIMENTO DA SBI
      - ~/workspace/repo/pessoal/odin:/var/www/html/wp-content/themes/odin
      - ~/workspace/repo/pessoal/sbi-odin-child:/var/www/html/wp-content/themes/odin-child
    environment:
      XDEBUG_CONFIG: "remote_host=localhost"
      PHP_IDE_CONFIG: "serverName=Docker"
    depends_on:
      - db

  db:
    image: mariadb
    env_file: .env
    ports:
      - ${MYSQL_PORT}:3306
    volumes:
      - ./.database/data:/var/lib/mysql

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    env_file: .env
    ports:
      - ${PMA_EXPOSED_PORT}:80
    links:
      - db

  mailhog:
    image: mailhog/mailhog
    env_file: .env
    ports:
      - ${MAILHOG_SMTP_PORT}:1025
      - ${MAILHOG_HTTP_PORT}:8025
